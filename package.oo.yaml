version: 0.0.1
scripts:
  bootstrap: |-
    npm install
    poetry install --no-root
    apt update && apt install -y ffmpeg
icon: ./icon.png
name: whisperx
repository: https://github.com/oomol-flows/whisperx.git
releaseNotes: init
displayName: "%display-name%"
description: "%key1%"
dependencies:
  ffmpeg: 0.3.8
  srt-lab: 0.1.4
